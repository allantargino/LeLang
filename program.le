program myProgram
{
	# variable declaration
	int a, b, c;
	int x, y;
	cte int d:=3, e:=3;
	int numero;
	decimal myDec;

	# program body
	a:=3+5+90;
	b:=4;
	myDec:=3.14f;

	Read(c);

	if(2 > c)
		numero:= a + c;
	endif
}