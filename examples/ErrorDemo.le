program myProgram
{
	# variable declaration
	int a, b, c;
	bool x;
	int a;
	cte int d:=89, e:=77;
	int numero;
	decimal myDec;

	# program body
	a:=3+5+90;

	myDec:=3.14f;
	myDec:="text";
       
	if(c>2 && a>2)
		Write("if1");
		if(myDec)
			Write("if2");
		else
			Write("else2");
		endif
	else
		Write("else1");
	endif
	
	Write(something);
	Read(d);

	while(c>2)
		Write("while ");
		c := c - 1;
	next

	d:= 8;
	randomWrongThing

	Write("Your Name");
	Write("The End");
}
